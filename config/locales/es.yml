#!/bin/env ruby
# encoding: utf-8
es:
  confirmation: "confirmación"
  views:
    pagination:
      previous: "&lt; Anterior"
      next: "SIguiente &gt;"
      truncate: "..."

helpers:
    label:
        password: "Contraseña"
